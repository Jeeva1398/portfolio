{
  "name": "jeeva-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Jeevaananthan M's developer portfolio - a static React site; contact messages and chat go through ZenithDesk.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "install:all": "npm install --prefix client",
    "build": "npm run build --prefix client"
  }
}
